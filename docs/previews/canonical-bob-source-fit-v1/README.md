# Canonical Bob source-fit proof v1

Review-only gate for replacing the code-drawn `hair-bob` builder with the three checked-in Bob SVG facings plus declarative head-fit envelopes.

- `01-six-head-directions.png`: current production Bob beside the SVG-derived proposal for all six heads and three authored facings.
- `02-literal-scale.png`: current (C) and source-derived (S) renders at literal 48 px and 32 px.
- `metrics.json`: source ownership, transform, coverage, and deterministic audit.

The proposal received explicit visual approval on 2026-08-02 and became the
input to the bounded production promotion. The proof artifacts and metrics
remain the historical pre-promotion record: at the time they were rendered,
they changed no compositor, export, canonical source SVG, or Unity path.
