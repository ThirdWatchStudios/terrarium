# QuotaCo open-pocket cross-junction accepted source

This external proof bank contains the owner-accepted fixed-light authored source
pair for `mask_15`: north, east, south, and west are connected while all four
diagonal crooks remain open floor pockets.

- `open_cross_junction-base.svg` owns one continuous green/plinth cross with
  exact accepted north, east, south, and west socket footprints.
- `open_cross_junction-upper.svg` owns one continuous cream four-way shell. The
  east-west frontage remains the visual owner through the hub while the north
  and south branches merge into it without a cap, pylon, or second inner face.
- The silhouette may be understood as the structural union of the accepted
  foreground and rear horizontal T controls, but the files are flattened
  authored art. They do not embed, link, transform, or stack either T source.
- All four concave pocket radii remain visible. No diagonal is filled, and no
  center plate or decorative rollover is introduced.
- The east and south service ticks retain the accepted per-cell seam ownership;
  no seam crosses the center of the hub.
- Existing local plane cues are preserved. Family-wide south-facing shade
  normalization remains a later polish pass and is not part of this acceptance.

The accepted gate includes the composed source at 240, 90, and 40 pixels per
cell, one-cell capped arms, long arms on light and dark floors, and both accepted
vertical straight facings. The center reads as one QuotaCo catalog junction, all
four sockets disappear into their neighbors, and every crook remains open floor.

This promotion accepts the external proof source and its direct `mask_15` ledger
mapping. It does not register a canonical production source, create a frame
identity, alter templates or the blob contract, route through the exporter,
commit an atlas, change schema, or add a Unity asset.
