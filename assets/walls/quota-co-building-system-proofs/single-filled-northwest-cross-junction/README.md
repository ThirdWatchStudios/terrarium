# QuotaCo single-filled northwest cross-junction accepted source

This external proof bank contains one owner-accepted fixed-light source pair for
`mask_37`: north, east, south, and west are connected; the northwest diagonal
is solid; and the northeast, southeast, and southwest crooks remain open floor.

- `open_cross_filled_nw-base.svg` owns one continuous lower four-socket
  structure. The northwest quadrant is buried structural mass while the other
  three crooks retain their accepted open-pocket socket footprints.
- `open_cross_filled_nw-upper.svg` owns one flattened cream union. It removes
  the buried north/west inner faces at source; `upper-shell` is the only cream
  path owner.
- The pair is authored directly in the east fixed-light register. It may use
  accepted `mask_19` geometry as a visual cue, but it is not an X mirror,
  rotation, filtered derivative, or alternate provenance for `mask_19`.
- No central cap, peak, post, badge, duplicate contour, duplicate material
  belt, hidden interior riser, or stacked junction survives at the hub.
- Acceptance was judged at 240, 90, and 40 pixels per cell in compact and
  1/3/6-cell arm compositions. Every installed neighboring row is accepted.

Owner acceptance promotes this pair only as the direct proof-layer provenance
for ledger row `mask_37`. The files do not register a canonical source or frame
identity, alter the blob contract, add an exporter or atlas route, change
schema, or authorize a Unity asset.
