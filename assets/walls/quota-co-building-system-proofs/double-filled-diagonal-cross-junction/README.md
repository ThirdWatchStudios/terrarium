# QuotaCo double-filled diagonal cross-junction accepted source

This external proof bank contains one owner-accepted, fixed-light source pair for
`mask_30`. North, east, south, and west are connected; the northeast and
southwest diagonals are solid wall; and the northwest and southeast crooks
remain open floor.

## Source ownership

- `open_cross_filled_ne_sw-base.svg` owns one continuous lower structural mass.
  Only the exposed southeast return carries green, face shade, contact shade,
  and the east/south service ticks.
- `open_cross_filled_ne_sw-upper.svg` owns the sole cream shell. Its S-shaped
  plane joins the northeast and southwest fills through the four-way hub while
  preserving the accepted northwest and southeast concave radii.
- Coral, green, and foreground shade exist only around the southeast return.
  The solid northeast and southwest quadrants expose no buried fascia,
  duplicate belt, interior rail, or false dividing seam.
- The north, east, south, and west sockets reach the exact cell boundaries.
  Cream remains the visible top owner at top-to-top joins; frontage treatment
  does not cross a buried socket.

The pair is authored directly as one fixed-light union. Its north outlet keeps
the west-authored register established by `mask_19`; its south outlet crosses
to the east register established by `mask_29`, with no runtime transform inside
the source. Those accepted pieces constrain the northeast and southwest closure
laws, but their SVGs are not stacked, linked, embedded, or used as alternate
cream owners. No center cap, peak, post, patch, overlay, or pair of touching
elbows survives in the source.

Owner acceptance promotes this pair only as the direct proof-layer source for
ledger row `mask_30`. It does not register a canonical source or frame identity,
alter the blob contract, add an exporter or atlas route, change schema, or
authorize a Unity asset.

The separately reviewed `mask_40` topology is now accepted as a proof-layer
derivation of this same pair. It omits only `base-boundary-seam` and
`upper-boundary-seam` before applying one whole-cell X mirror, so the adjoining
west cell remains the sole shared-socket seam owner. That acceptance adds no
companion SVG or source bank and does not promote either mask into canonical,
exporter, atlas, schema, blob, or Unity registration.
