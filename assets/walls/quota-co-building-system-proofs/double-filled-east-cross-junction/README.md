# QuotaCo double-filled east cross-junction accepted proof source

This external proof bank contains one owner-accepted, fixed-light source pair
for `mask_25`. North, east, south, and west are connected; the northeast and
southeast diagonals are solid wall; and the northwest and southwest crooks
remain open floor.

- `open_cross_filled_e-base.svg` owns one flattened lower structure. The entire
  east half is buried structural mass, while only the exposed southwest frontage
  carries green, shade, and contact cues.
- `open_cross_filled_e-upper.svg` owns one continuous cream union. Its two west
  crook radii admit the west branch into the solid two-cell-wide north/south
  slab without exposing a buried east fascia. The short upper shoulder shade
  hands off at `y=97`, leaving the base as sole shade owner below the crook so
  the 40 px turn does not collapse into a doubled dark bar.
- Accepted `mask_15`, `mask_19`, `mask_23`, and `mask_24`/`mask_42` are boundary
  controls only. Their SVGs are not stacked, linked, transformed, or embedded.
- The compact occupancy is `.## / ### / .##`, producing
  `20 26 / 2 25 42 / 16 34`. Longer evidence extends the accepted `24/42`
  slab north and south and the ordinary branch west.
- Extending the horizontal branch east beyond the adjacent `mask_42` column
  would introduce separate synthetic companion `mask_43`; that topology is
  explicitly deferred.
- No east-side coral or green fascia, center seam, cap, peak, post, patch,
  overlay, duplicate cream owner, or duplicate material belt is permitted.

This pair is accepted as `direct-reuse` / `accepted-source-mapping` proof-layer
provenance for `mask_25` in the equal-height 47-mask ledger. Acceptance does not
register a canonical source or frame identity, alter templates or the blob
contract, add an exporter or atlas route, change schema, or authorize a Unity
asset. Companion `mask_43` remains synthetic and outside this acceptance.
