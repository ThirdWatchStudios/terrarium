# QuotaCo single-filled cross-junction accepted source

This external proof bank contains one owner-accepted fixed-light source pair for
`mask_19`: north, east, south, and west are connected; the northeast diagonal
is solid; and the southeast, southwest, and northwest crooks remain open floor.

- `open_cross_filled_ne-base.svg` owns one continuous lower four-socket
  structure. The northeast quadrant is buried structural mass, while the east,
  south, and west frontage keeps the accepted `mask_15` socket footprints and
  service-seam ownership.
- `open_cross_filled_ne-upper.svg` owns one flattened cream union. Its
  northeast fill removes the buried north/east inner faces at source, while
  the other three crooks retain the accepted concave radii.
- The outgoing west/east foreground register and north/south spine are one
  authored junction, not `mask_17`, `mask_18`, or `mask_15` stacked together.
  No central cap, peak, post, badge, duplicate contour, or duplicate material
  belt survives at the hub.
- The northwest turn follows the accepted `mask_18` open-crook law. The
  southeast turn follows the accepted `mask_17` open-crook law. The southwest
  turn and all four cardinal sockets remain constrained by accepted
  `mask_15`.
- The northeast area is a continuous cream wall top. Buried coral, green,
  charcoal-face, and service-seam geometry is absent from the visible result
  rather than presented as an interior fascia.
- Every installed `mask_19` proof keeps the canonical west fixed-light socket
  register through its north and south neighbors. Mirroring only those ordinary
  vertical neighbors produces a false lateral step; the east-register
  northeast-filled cross is the separate `mask_37` topology row, not a
  transformed `mask_19` variant.
- The accepted 2026-07-25 consistency correction phases the right-hand cream
  plane, light arris, dimensional shade, and arris seam as parallel nested
  curves into one combined coral/green return. Duplicate south-branch belt
  owners are forbidden; the accepted combined paths close behind the turn
  while all four full-resolution socket boundaries remain pixel-identical.

Owner acceptance promotes this pair only as the direct proof-layer provenance
for ledger row `mask_19`. It does not derive another facing, register a
canonical source or frame identity, alter the blob contract, add an exporter or
atlas route, change schema, or authorize a Unity asset.
