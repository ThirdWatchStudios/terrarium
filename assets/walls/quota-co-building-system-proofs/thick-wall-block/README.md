# Equal-height thick-wall block accepted proof sources

This directory contains the two owner-accepted fixed-light proof-source pairs
for a completely occupied 2×2 wall block:

- `filled_nw_elbow-*` authors mask 20 on the rear/west side.
- `filled_sw_elbow-*` authors mask 16 on the foreground/west side.

Masks 26 and 34 are accepted proof-layer whole-cell X mirrors of those sources. The
southeast derivation applies the accepted boundary-seam omission before its
mirror, so seam ownership remains with the adjoining foreground cell.

The foreground upper retains the standard fixed-light south-face step: a
black `0.08` material shade from `y=63..88`, immediately above the coral band.
It is a plane-separation cue, not a cast shadow, and mirrors unchanged into
mask 34.

These SVGs are accepted proof sources and ledger mappings only. They are not
registered production stems, exporter inputs, atlas frames, schema values, or
Unity assets.
